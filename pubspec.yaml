name: custom_drop_down
description: A new Flutter package project.
version: 0.1.0
homepage: https://github.com/Snapp-X/custom_drop_down

environment:
  sdk: '>=3.0.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
